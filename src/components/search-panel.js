import React from "react";

const SearchPanel = () => {
    const searchStyle = {
        fontSize: "22px"
    };
    return <input style={searchStyle} placeholder="search" />;
};
export default SearchPanel;
